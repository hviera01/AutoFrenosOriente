name: sistema_ventas
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.2
  firebase_core: ^4.11.0
  cloud_firestore: ^6.6.0
  firebase_auth: ^6.5.4
  go_router: ^17.3.0
  intl: 0.20.2
  uuid: ^4.5.3
  crypto: ^3.0.7
  shared_preferences: ^2.5.5
  google_fonts: ^8.1.0
  excel: ^4.0.6
  archive: ^3.6.1
  printing: ^5.14.3
  pdf: ^3.12.0
  barcode: ^2.2.9
  path_provider: ^2.1.6
  http: ^1.2.2
  open_file: ^3.5.10
  share_plus: ^12.0.2
  file_picker: ^11.0.2
  fl_chart: ^0.69.0
  image: ^4.1.2
  mobile_scanner: ^7.1.2
  esc_pos_utils_plus: ^2.0.4
  qr_flutter: ^4.1.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  assets:
    - assets/images/logo.jpg
    - assets/images/logo_redondo.png
    - assets/images/fondo.jpg
    # Carpeta completa (no archivo por archivo): así GoogleFonts.getFont()
    # los encuentra solo escaneando el manifest de assets, sin red -ver nota
    # grande más abajo, junto a "fonts:"-.
    - assets/fonts/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

  # Copias locales de las tipografías de tipografiasDisponibles (ver
  # core/services/tipografia_service.dart), bajadas de github.com/google/fonts
  # con el mismo nombre de archivo que usa la API de Google Fonts (Family-
  # Regular.ttf, Family-Bold.ttf, etc.) -eso es lo que permite que
  # GoogleFonts.getFont() las encuentre solas escaneando los assets, sin
  # tocar la red, como documenta el propio paquete ("Bundling fonts when
  # releasing" en su README-. Antes de esto, elegir una tipografía que el
  # dispositivo nunca hubiera descargado no cambiaba nada en pantalla, porque
  # GoogleFonts.config.allowRuntimeFetching está en false a propósito (ver
  # main.dart: la app corre en cajas/POS con internet lento o intermitente).
  # Montserrat/Nunito/Roboto/Inter solo existen como fuente variable en el
  # repo de Google Fonts (un solo archivo cubre todos los grosores); por eso
  # el mismo archivo se repite bajo cada nombre de grosor.
  fonts:
    - family: Poppins
      fonts:
        - asset: assets/fonts/Poppins-Regular.ttf
        - asset: assets/fonts/Poppins-Medium.ttf
          weight: 500
        - asset: assets/fonts/Poppins-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Poppins-Bold.ttf
          weight: 700
        - asset: assets/fonts/Poppins-ExtraBold.ttf
          weight: 800
    - family: Montserrat
      fonts:
        - asset: assets/fonts/Montserrat-Regular.ttf
        - asset: assets/fonts/Montserrat-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Montserrat-Bold.ttf
          weight: 700
        - asset: assets/fonts/Montserrat-ExtraBold.ttf
          weight: 800
    - family: Nunito
      fonts:
        - asset: assets/fonts/Nunito-Regular.ttf
        - asset: assets/fonts/Nunito-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Nunito-Bold.ttf
          weight: 700
        - asset: assets/fonts/Nunito-ExtraBold.ttf
          weight: 800
    - family: Roboto
      fonts:
        - asset: assets/fonts/Roboto-Regular.ttf
        - asset: assets/fonts/Roboto-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Roboto-Bold.ttf
          weight: 700
        - asset: assets/fonts/Roboto-ExtraBold.ttf
          weight: 800
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-Regular.ttf
        - asset: assets/fonts/Inter-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Inter-Bold.ttf
          weight: 700
        - asset: assets/fonts/Inter-ExtraBold.ttf
          weight: 800

# Ícono de la app: cuadrado en Android (assets/images/logo_icono.png -solo
# el disco+pinza, sin el texto del lockup completo, que se vuelve ilegible a
# tamaño de ícono-, el propio launcher de Android se encarga de la forma
# final según el equipo), redondo con fondo transparente en Windows
# (assets/images/logo_redondo.png). Ambos generados con
# tool/generar_logo_icono.dart y tool/generar_logo_redondo.dart, en ese orden.
flutter_launcher_icons:
  android: "launcher_icon"
  image_path: "assets/images/logo_icono.png"
  windows:
    generate: true
    image_path: "assets/images/logo_redondo.png"
    icon_size: 256
